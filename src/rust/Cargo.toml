[package]
name = 'schedule'
version = '0.1.0'
edition = '2021'

[lib]
crate-type = [ 'staticlib' ]
name = 'schedule'

[dependencies]
extendr-api = '*'
home = "0.5.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
